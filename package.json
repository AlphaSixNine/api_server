{
  "name": "api_server",
  "version": "1.0.0",
  "description": "Twilio api server",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Neil",
  "license": "MIT",
  "dependencies": {
    "env": "^0.0.2",
    "express": "^4.19.2",
    "twilio": "^5.2.2"
  }
}
